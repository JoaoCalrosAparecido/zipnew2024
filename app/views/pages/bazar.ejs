<!DOCTYPE html>
<html lang="pt_br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bazar ZIP</title>
    <link rel="stylesheet" href="../css-bazar/allBazar.css">
</head>

<body>
    <!-- bazar-corpo -->

    <section id="AllBazar">
        <% if (bazaarList) { %>
            <% Object.keys(bazaarList).forEach(function (bazaarId) { %>
                <section id="imageSection">
                    <section id="imagePreview"></section>
                    <img id="imagePreview" src="/IMG/uploads/<%= bazaarList[bazaarId].imgBazar %>" alt="Imagem do Bazar"
                        class="img-bazar">
                    <section id="formOverlay">
                        <p id="Name">
                            <%= bazaarList[bazaarId].nome %>
                        </p>
                        <p id="Year">
                            <%= bazaarList[bazaarId].ano %>
                        </p>
                        <p id="Description">
                            <%= bazaarList[bazaarId].descricao %>
                        </p>
                    </section>
                </section>

                <section id="additionalInputs">
                    <p id="titulo">
                        <%= bazaarList[bazaarId].titulo %>
                    </p>
                </section>

                <section id="textAreaSection">
                    <p id="bio">
                        <%= bazaarList[bazaarId].biografia %>
                    </p>
                </section>

                <section id="cen">
                    <% bazaarList[bazaarId].produtos.forEach(function (produto) { %>
                        <a id="none" href="/produtos/<%= produto.id_prod_cliente %>">
                            <section id="ash">
                                <img id="addyy" src="../../IMG/uploads/<%= produto.img1 %>" alt="Imagem do produto">
                            </section>
                            <p id="cust">
                                <%= produto.tituloprod %>
                            </p>
                            <p id="cust">R$ <%= produto.preçoprod %>
                            </p>
                        </a>
                        <% }) %>
                </section>


                </article>
    </section>
    <% }) %>
        <% } %>
            </section>

            
</body>

</html>